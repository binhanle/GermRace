using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;

public class GameController : MonoBehaviour {

	// Use this for initialization
	void Start () {
        // let's make a tile
        //GameObject tile = Instantiate((GameObject)Resources.Load("Prefabs/Tile", typeof(GameObject)));
        //EditorUtility.DisplayDialog("test", tile.GetComponent<Tile>().getPos().ToString(), "ok", "cancel");
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
